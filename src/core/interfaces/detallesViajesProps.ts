export type ViajePasajeroProps = {
  full_name: string;
  orden_recogida: number;
  orden_entrega: number;
  pmcp_es_origen: boolean;
};

export type viajeProps={
    id: number;
    punto_concentracion: string;
}